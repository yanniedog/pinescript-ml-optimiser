numpy>=1.24.0
pandas>=2.0.0
optuna>=3.4.0
requests>=2.31.0
matplotlib>=3.7.0
# Plotly/Dash for interactive charts (optional but recommended)
plotly>=5.18.0
dash>=2.14.0
# Performance optimization
psutil>=5.9.0
# JIT compilation for 5-10x speedup (optional but highly recommended)
# Install with: pip install numba
# numba>=0.58.0
# Note: Numba requires LLVM and may need additional setup on some systems
# The code will fall back to pure NumPy if Numba is not available
